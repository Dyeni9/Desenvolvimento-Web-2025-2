// Espera o conteúdo da página ser totalmente carregado
window.addEventListener('DOMContentLoaded', (event) => {
    // Encontra o nosso container e adiciona a classe para iniciar a animação
    const container = document.querySelector('.curriculo-container');
    container.classList.add('pagina-visivel');
});